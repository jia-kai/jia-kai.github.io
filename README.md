This is [my website](https://jiakai.xyz) based on Jekyll minima 2.5.1.

Jekyll automatically generates `sitemap.xml`, `robots.txt`, and JSON-LD metadata
for posts during each build. New posts only need a title and date in their front
matter; the post layout is configured as a collection default.
